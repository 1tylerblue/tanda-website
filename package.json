{
  "name": "tanda-website-verification",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "generate": "node scripts/render-promotions.cjs",
    "test": "node --test tests/*.test.cjs && npm --prefix backend test && node scripts/render-promotions.cjs --check",
    "test:browser": "playwright test",
    "preview": "node scripts/preview.cjs"
  },
  "devDependencies": { "@playwright/test": "1.63.0", "@axe-core/playwright": "4.11.0" }
}
